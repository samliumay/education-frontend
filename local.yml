version: '3'

services:
  frontend-prod:
    build:
      context: .
      dockerfile: ./docker/prod/Dockerfile
      args:
        VITE_API_URL: https://api.clavis.the-o.co
    image: frontend
    restart: unless-stopped
    ports:
      - '3000:3000'
    expose:
      - '80'
